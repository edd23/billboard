<h1>Artists</h1>

  <% @artists.each do |artist| %>
  <h2><%= artist.name %></h2>
  <%= link_to 'Show Artist', artist_path(artist) %>
  <%= link_to 'New Artist', new_artist_path %>
  <%= link_to 'Edit', edit_artist_path(artist) %>
  <%= link_to 'Delete', artist, method: :delete %>
  <% end %>


